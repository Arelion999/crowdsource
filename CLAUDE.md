# Как здесь работать

Проект — народный русский перевод Guild Wars 2. Подробный порядок работы для
участников лежит в [README.md](README.md); здесь — то, что нужно держать в
голове с первой минуты.

## 1. Иерархия правок: батчи → bin → граф

**`dictionary.bin` руками не правим никогда.** Он собирается из батчей.

```
правим батчи (crowdsource/<категория>/<имя>.csv)
  -> python tools/dict_tool.py frombatches --batches-win --apply
  -> python tools/index.py build          # граф пересобрать
```

Причина в [README §«Порядок работы»](README.md#порядок-работы-батчи--источник-истины):
батчи в git, у них есть история и откат; bin весит больше сотни мегабайт, в git
не лежит и правится необратимо. Разъезд между ними не видит ни один линтер —
обе стороны сами по себе чисты.

У каждой пишущей по bin команды есть парная по батчам (`canon` → `canonbatches`,
`typo` → `tools/typofix.py`, `combat.py apply` → `combat.py batches`,
`gw2s_import.py apply` → `gw2s_import.py batches`). Звать надо парную. Если
правка всё же легла в bin первой — довести её до батчей в тот же заход, иначе
следующая сборка её сотрёт.

Только в bin живут: слой имён `pn_*` (`dict_tool.py pnadd` / `pnset`) и русские
имена словарей (`dict_tool.py dictnames`). Всё остальное — в батчах.

## 2. Обязательные гейты

| когда | что запустить | что должно получиться |
|---|---|---|
| перед сдачей батча | `python validate.py <файл>` | 0 ошибок |
| заливка переводов | `python tools/batch_tool.py fill <батч> <json>` | сам делает бэкап в `.batch_bak/` и сверяет плейсхолдеры и колонку english |
| после записи в bin | `python tools/charscan.py compare .dict_bak/<последний бэкап>` | по незатронутым классам **+0, регрессий 0** |
| после правки bin | дописать раздел в [DEFECTS.md](DEFECTS.md) | состояние и остаток по каждому классу |
| сдача батча | `batch_tool.py claim <батч> --who <ник>`, затем `python stats.py --mark-done` | ✅ в [CLAIMS.md](CLAIMS.md) |

`charscan compare` идёт минуты — это нормально, не прерывать.

## 3. Канон терминов — в этом порядке

1. [GLOSSARY.md](GLOSSARY.md);
2. **человеческий корпус** — батчи, у которых в CLAIMS.md в колонке «Кто взял»
   не написано «Автозаполнение». Машинные батчи каноном не являются;
3. вики;
4. своё решение — и сразу в GLOSSARY.md, чтобы стало прецедентом.

Для названий предметов: тип вещи берётся из `/v2/items` через
`tools/itemfacts.py` (Targe — щит, Greaves — сапоги), прецеденты людей —
через `tools/index.py proof`. Названия сетов брони — из человеческих батчей
гардероба `ui_003…ui_007`, а не из головы.

Всё, что игрок может пойти гуглить (персонажи, локации, предметы, облики,
умения, титулы, фракции), обязано лежать в слое `pn_*` — смысл слоя в
обратимости, а не в переводе.

## 4. Прежде чем переводить руками

Ручной перевод — последний шаг, а не первый:

1. **подстановка из корпуса** — строка уже могла встретиться в другом батче;
   совпадения переносим как есть, это заодно держит единообразие;
2. **генерация однотипных семей по правилу** — названия предметов, статы,
   материалы и прочее собираются шаблоном из уже утверждённых кусков
   (на `items_024` так закрылось 479 строк из 500 без единого нового термина);
3. и только остаток — руками.

Порядок слов в названиях предметов задан человеческими батчами `items_013` и
`items_014`: прилагательное-сет + предмет + суффикс + стат-префикс, а материал
(«из тёмной стали») идёт хвостом после стат-префикса.

Если в колонке `english` лежит не английский, а немецкий, испанский или
французский текст (частая беда батчей `new_034` и дальше) — это не строка на
перевод. Хеш считается от испорченного english, такая запись в игре мертва:
её надо удалить, а не переводить.

## 5. Типографика

* многоточие пишем **как в оригинале**: `...` в оригинале — `...` в переводе;
* кавычки внешние «ёлочки», внутренние „лапки“;
* ё расставляем;
* где в английском `[s]` или `[pl:"…"]` — в переводе группа форм
  `[форма1|форма2|форма3]`, и основу к ней приклеивать нельзя
  («союзника[|а|ов]» проходит линтер и ломается в игре);
* строки не на английском (французские хвосты клиента) не переводим, а удаляем —
  `tools/langfilter.py check` / `apply`.

## 6. Грабли, на которые уже наступали

* **`csv_to_bin.py` не разбирает аргументы.** Любой запуск без явной папки —
  включая `--help` — собирает bin из пустого места и затирает 121 МБ. Не звать
  вовсе: путь в bin один, через `frombatches`.
* **`index.py build` падает с PermissionError**, пока запущен `tools/graphserve.py`
  — он держит `sync/index.db`. Сначала остановить сервер.
* **Репозиторий `crowdsource` публичный.** Адрес сервиса выкачки строк намеренно
  не в коде: `GW2_SYNC_URL` или `sync/endpoint.txt` (в .gitignore). Не печатать,
  не коммитить, не цитировать.
* **Бэкапы bin** — `.dict_bak/`. Имя файла — время запуска, который его создал,
  содержимое — состояние ДО этого запуска.
* Категория словаря = группа отключения в игре. Названия и описания разведены
  (`item_names` / `item_descriptions` и так далее), новые словари не создаём.

## 7. Карта документов

| файл | о чём |
|---|---|
| [README.md](README.md) | полный порядок работы, как переводить, как сдавать |
| [GLOSSARY.md](GLOSSARY.md) | канон терминов |
| [CLAIMS.md](CLAIMS.md) | доска захвата батчей, кто что взял, что вычитано |
| [DEFECTS.md](DEFECTS.md) | реестр классов дефектов bin: состояние, чем чинили, остаток |
